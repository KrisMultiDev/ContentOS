{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core IPC and native folder picker",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
